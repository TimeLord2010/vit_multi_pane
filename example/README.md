# vit_multi_pane example

Runnable demo of the `vit_multi_pane` package. The FAB manages pages
(add, replace, go to next) through a `VitMultiPaneController`.

See the package root `README.md` for usage.
