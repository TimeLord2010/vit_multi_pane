name: vit_multi_pane
description: >-
  Layout de múltiplos painéis: páginas lado a lado, com divisória
  arrastável e limites de largura por painel.
version: 2.0.0
homepage:
repository: https://github.com/TimeLord2010/vit_multi_pane

environment:
  sdk: ^3.12.2
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
